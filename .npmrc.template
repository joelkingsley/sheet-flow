# NPM Registry Configuration Template
# Copy this to .npmrc and add your GitHub token for private package access

# For sponsors with access to private packages:
# @joelkingsley:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=YOUR_GITHUB_TOKEN_HERE

# For non-sponsors, no additional configuration needed
# The app will use the WebView fallback implementation
